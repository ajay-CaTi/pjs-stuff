// Garbage collector inV8 engine automatically we have no control over them
// runs backgound

// Syntax

// Math.random() * (max - min) + min;

console.log(Math.floor(Math.random() * (10 - 5) + 5));
